
let REST_ENDPOINT: string;
let MASTER_OTP: string;

MASTER_OTP = '030621'

REST_ENDPOINT = "http://parkaro.orbitz.host/api"

const Config = {
    REST_ENDPOINT,
    MASTER_OTP
};

export default Config;
